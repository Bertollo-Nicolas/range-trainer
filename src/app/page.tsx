import { HomeClient } from './home-client'

export const metadata = {
  title: 'Range Editor - Range Trainer',
  description: 'Create and edit poker ranges with visual tools',
}


export default function Home() {
  return <HomeClient />
}
